version: "2"
project:
  id: "url-shortener"
sql:
  - engine: "postgresql"
    queries: "./db/queries"
    schema: "./db/migrations"
    gen:
      go:
        package: "db"
        out: "./db/sqlc"
        emit_empty_slices: true
        emit_json_tags: true
        json_tags_case_style: camel
